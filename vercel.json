{
  "version": 2,
  "buildCommand": "cd resolve_accouting && npm install && npm run build",
  "outputDirectory": "resolve_accouting/dist",
  "installCommand": "npm install",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
